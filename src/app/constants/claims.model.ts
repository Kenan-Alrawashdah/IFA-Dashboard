export interface ClaimsModel {
    unique_name: string;
    role:        string;
    email:       string;
    FullName:string;

    // nbf:         number;
    // exp:         number;
    // iat:         number;
}